# mBERT Fine-tuning — Cross-lingual NER
Cross-lingual named entity recognition via multilingual BERT fine-tuning.
### ■ Research Highlights
- **mBERT Fine-Tuning**: Fine-tunes `bert-base-multilingual-cased` on English CoNLL-2003, then zero-shot tran- **Token-Level Classification**: Uses a BIO tagging scheme with WordPiece-aware label alignment to handle su- **Algorithms Focus**: Analyses cross-lingual transfer gap by entity type (PER/LOC/ORG), showing Latin-scrip**Tech Stack:** Python, `Transformers`, `datasets`.
